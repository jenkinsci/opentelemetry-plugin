---
version: "2.4"
networks:
    default:
        name: integration-testing-es
services:
    elasticsearch:
        extends:
            file: elastic-stack.yml
            service: elasticsearch
    wait:
        image: busybox
        depends_on:
            elasticsearch: { condition: service_healthy }
